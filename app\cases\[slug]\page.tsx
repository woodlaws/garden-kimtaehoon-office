import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Breadcrumbs, CTA, PageHero } from "@/components/Common";
import { caseExamples, services } from "@/data/site";
export function generateStaticParams(){return caseExamples.map(c=>({slug:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=caseExamples.find(v=>v.slug===slug);return {title:c?.title??"업무 진행 예시",robots:{index:false,follow:true}}}
export default async function CaseDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=caseExamples.find(v=>v.slug===slug);if(!c)notFound();const service=services.find(s=>s.title.includes(c.category.replace("각종 ","")))??services[0];return <><PageHero eyebrow={c.status} title={c.title} description={c.issue}/><Breadcrumbs items={[{label:"업무 사례",href:"/cases"},{label:c.title}]}/><section className="section"><div className="shell article-narrow"><div className="sample-notice"><strong>실제 사건이 아닌 설명용 예시입니다.</strong><p>결과를 보장하거나 특정 사건의 처리 실적을 나타내지 않습니다.</p></div><section><h2>의뢰 배경 예시</h2><p>신청 또는 처분 관련 문서를 받은 뒤 어떤 자료를 준비하고 어느 절차부터 확인해야 하는지 상담을 요청한 상황을 가정합니다.</p></section><section><h2>주요 확인 쟁점</h2><ul><li>관할 기관과 적용되는 행정절차</li><li>신청 또는 불복 기한</li><li>이미 제출한 자료와 추가로 필요한 증빙</li><li>기관이 안내한 보완 항목</li></ul></section><section><h2>진행 과정 예시</h2><ol><li>처분서·안내문과 받은 날짜 확인</li><li>사실관계와 신청 목적 정리</li><li>요건과 기한, 구비서류 검토</li><li>업무 범위와 예상 일정 안내</li><li>서류 작성 및 보완 대응 지원</li></ol></section><section><h2>행정사의 조언</h2><p>기관 문서는 일부만 보지 말고 전체를 보관하고, 안내된 제출 기한을 먼저 확인하는 것이 좋습니다. 개별 사안은 사실관계에 따라 달라질 수 있으므로 관련 자료를 함께 검토해야 합니다.</p></section><div className="related-service"><small>관련 업무</small><h2>{service.title}</h2><p>{service.intro}</p><Link className="button outline" href={`/services/${service.slug}`}>업무 안내 보기</Link></div></div></section><CTA/></>}
