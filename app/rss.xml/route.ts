import {blogPosts} from "@/data/site";
export const dynamic="force-static";
export function GET(){const base=process.env.NEXT_PUBLIC_SITE_URL||"https://kim-taehoon-administrative-office.geosangbruce.chatgpt.site";const esc=(v:string)=>v.replace(/[<>&'"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]!));const items=blogPosts.map(p=>`<item><title>${esc(p.title)}</title><link>${base}/blog/${p.slug}</link><guid>${base}/blog/${p.slug}</guid><pubDate>${new Date(p.date).toUTCString()}</pubDate><description>${esc(p.summary)}</description></item>`).join("");const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>김태훈 행정사 블로그</title><link>${base}/blog</link><description>행정 절차와 준비사항을 쉽게 설명합니다.</description><language>ko-KR</language>${items}</channel></rss>`;return new Response(xml,{headers:{"Content-Type":"application/rss+xml; charset=utf-8"}})}
